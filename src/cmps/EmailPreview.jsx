/* eslint-disable react/prop-types */
import { utilService } from "../services/util.service";

export function EmailPreview({ email, onDeleteEmail }) {
  return <section className="emailpreview">EmailPreview</section>;
}
